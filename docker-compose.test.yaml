services:
    api:
      container_name: test_api
      environment: 
        - DB_NAME=projects
        - DB_CONNECTION=mongodb://elwin:5lw9n@db:27017
        - NODE_ENV=test
      depends_on:
        - db
      tty: true
    db:
      container_name: test_db
      volumes:
        - "test_db:/var/lib/mongodb/data"
      environment:
        - MONGO_INITDB_DATABASE=projects_test
        - MONGO_INITDB_ROOT_PASSWORD=5lw9n 
  volumes:
    test_db:
  networks: 
    default:
      name: app_test